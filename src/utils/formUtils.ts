// src/utils/formUtils.ts
// This file is now empty. Its contents have been refactored into:
// - src/utils/idUtils.ts
// - src/utils/dataCleaners.ts
// - src/features/contacts/utils/contactFormMappers.ts
// - src/features/contacts/utils/socialMediaFormatters.ts

const formUtilsPlaceholder = {};
export default formUtilsPlaceholder;

// Keeping export {} in case any old named imports are still being resolved by module path,
// though they would fail as the named exports are gone.
// The default export above is to address the specific "doesn't provide an export named: 'default'" error.
export {};